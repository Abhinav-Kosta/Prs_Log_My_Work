<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Log My Work</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="icon" href="/images/logo.webp" type="image/webp">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600&display=swap"
        rel="stylesheet">
</head>
<body>
    <div class="login-wrapper">
        <div class="login-content">
            <div class="login-image">
                <img src="/images/login.jpg" alt="Log My Work Logo">
            </div>
            
            <div class="login-form">
                <%- include("../includes/flash.ejs") %>
                <h2><%= role.charAt(0).toUpperCase() + role.slice(1) %> Login</h2>

                <form action="/login/<%= role %>" method="POST">
                    <div class="form-group">
                        <input type="text" name="facultyId" placeholder="Faculty Id" required>
                    </div>
                    <div class="form-group">
                        <input type="password" name="password" placeholder="Password" required>
                    </div>
                    <div class="form-group">
                        <button type="submit">Login as <%= role %></button>
                    </div>
                </form>

                <p>Not a <%= role %>? Login as:
                    &nbsp;&nbsp;
                    <% if (role !== "faculty") { %>
                        <a href="/login/faculty">Faculty</a>
                    <% } %>
                    &nbsp;&nbsp;&nbsp;&nbsp;
                    <% if (role !== "admin") { %>
                        <a href="/login/admin">Admin</a>
                    <% } %>
                    &nbsp;&nbsp;&nbsp;&nbsp;
                    <% if (role !== "hoi") { %>
                        <a href="/login/hoi">HOI</a>
                    <% } %>
                </p>
                <hr>
                <h6 style="text-align: center;">Developed By <b>Abhinav Kosta</b></h6>
                <h6 class="text-muted mb-3">Under the supervision of: <b>Dr. Manish Gupta</b></h6>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
    <script src="/js/validate.js"></script>
</body>
</html> 